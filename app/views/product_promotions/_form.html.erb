<%= form_with(model: product_promotion) do |form| %>
  <% if product_promotion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_promotion.errors.count, "error") %> prohibited this product_promotion from being saved:</h2>

      <ul>
        <% product_promotion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :promotion_id, style: "display: block" %>
    <%= form.text_field :promotion_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
